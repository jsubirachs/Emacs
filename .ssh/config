# File with 'chmod 600 config' permissions

PermitLocalCommand true

# An entry in ~/.ssh/config for github can be useful if you are using a github-specific key pair.
Host github.com
    User git
    IdentityFile ~/.ssh/github-jsubirachs
    # LocalCommand ssh-add -t 1d ~/.ssh/github-jsubirachs
